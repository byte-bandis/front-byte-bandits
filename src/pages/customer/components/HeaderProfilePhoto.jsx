import styled from "styled-components";
import CustomPhoto from "../../../components/shared/CustomPhoto";

const HeaderProfilePhoto = styled(CustomPhoto)`
  width: 100%;
  height: 300px;
  object-fit: cover;
  margin-top: 2rem;
`;

export default HeaderProfilePhoto;
